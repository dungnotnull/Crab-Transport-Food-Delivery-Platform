# Global Bug & Issue Tracking

Tài liệu này được sử dụng làm trung tâm kiểm soát lỗi. Mọi bug, issue, lỗ hổng hoặc các vấn đề thiết kế phát sinh trong quá trình phát triển (thuộc cả Frontend, Backend, hoặc Infrastructure) đều được Agent log (ghi nhận) tại đây.

| ID | Date | Component (FE/BE/Infra) | Description | Severity | Status (Open/In Progress/Resolved) | Resolution / Notes |
# Global Bug & Issue Tracking

Tài liệu này được sử dụng làm trung tâm kiểm soát lỗi. Mọi bug, issue, lỗ hổng hoặc các vấn đề thiết kế phát sinh trong quá trình phát triển (thuộc cả Frontend, Backend, hoặc Infrastructure) đều được Agent log (ghi nhận) tại đây.

| ID | Date | Component (FE/BE/Infra) | Description | Severity | Status (Open/In Progress/Resolved) | Resolution / Notes |
|:---|:---|:---|:---|:---|:---|:---|
| BUG-001 | 2026-08-19 | BE | Lỗ hổng Validation DTO: `BookOrderDto` thiếu `@ValidateNested()`, dẫn đến việc không chặn được tọa độ rác `lat/lng` lồng bên trong. | High | Resolved | Đã thêm `@ValidateNested` và `Type` từ `class-transformer` để siết chặt đầu vào. |
| BUG-002 | 2026-08-19 | BE/Thiết kế | Thiếu luồng API Preview: Khách hàng cần xem trước đường vẽ và cước phí *trước khi* nhấn Đặt cuốc. API `/book` gom cả việc tính giá lẫn tạo cuốc làm mất trải nghiệm UX. | Medium | Resolved | Đã tách riêng endpoint `POST /api/v1/orders/preview` gọi tới OSRM và Pricing mà không lưu database. |
| BUG-003 | 2026-08-19 | BE/Tài liệu | Thiếu Document API Driver: Các API cập nhật vị trí và bật/tắt online của Tài xế chưa được ghi chú vào `API-CONTRACT.md`. | Low | Resolved | Đã bổ sung API 2.8 và 2.9 vào Source of Truth để Frontend gọi. |
| BUG-004 | 2026-08-19 | BE/Real-time | Cronjob hủy đơn tự động không phát Socket: Frontend bị kẹt mãi mãi ở trạng thái `FINDING_DRIVER` khi Backend đã hủy đơn vì timeout. | High | Resolved | Đã thêm logic bắn `order:status_changed` tới `trackingGateway` trong file `orders.service.ts` khi cronjob hủy cuốc. |
| BUG-005 | 2026-08-19 | Infra/Dev | Xung đột cổng Port 3000 giữa Frontend Vite và Backend NestJS: File `.env` ban đầu để Backend `PORT=3000` trùng với Vite dev server `port: 3000`, dẫn đến Vite Proxy trả về lỗi 500 `ECONNREFUSED`. | High | Resolved | Đã tách cổng chuẩn: Backend chạy cổng `PORT=4000` và Frontend Vite chạy cổng `PORT=3000` (Proxy `/api` về 4000). Toàn bộ Auth/DB đã hoạt động 100%. |
| BUG-006 | 2026-08-20 | FE | Màn hình DriverDashboard không hiển thị thông tin xe (vehicle_brand) và hình ảnh xe (vehicle_image) thực tế. UI đang render icon `<Car />` và xài data giả do Backend cũ không trả về profile. | Medium | Resolved | Đã hiển thị thẻ `<img />` load URL từ `authStore.user.driverProfile.vehicle_image` và render dữ liệu thật của xe. |
| BUG-007 | 2026-08-20 | FE | Thiếu kết nối Real-time Socket: Luồng Booking ở Frontend hiện đang xài UI mock (`handleAdvanceStatusMock`) để chuyển status trip, thay vì gọi API Backend và lắng nghe sự kiện qua WebSocket (mặc dù đã cài `socket.io-client`). | Critical | Resolved | Đã khởi tạo kết nối `socket.io` tới Gateway bằng `socket.service.ts`, lắng nghe các event `trip:status_changed`, `driver:trip_offer` và `driver:update_location`. |
| BUG-008 | 2026-08-20 | FE | Thiếu sót nghiêm trọng về UI/UX trang Driver: Không có Bản đồ (Map) chỉ đường, không gọi định vị GPS thật, hiển thị cuốc xe mock thay vì lắng nghe Socket thật, và thiếu hẳn trang quản lý Lịch sử cuốc/Doanh thu. | High | Resolved | Đã tích hợp `react-leaflet`, xin quyền `navigator.geolocation`, gọi API lấy chi tiết chuyến đi, lịch sử chuyến đi, và render giao diện hoàn chỉnh có Map, và lấy ví từ API thật. |
| BUG-009 | 2026-08-20 | FE | Thiếu sót nghiêm trọng về UI/UX trang Customer: Vẫn xài giả lập Status Trip (lỗi BUG-007), không có cách nào khôi phục cuốc xe đang chạy nếu User vô tình F5/Reload trang, và thiếu màn hình Lịch sử chuyến đi/Hồ sơ cá nhân. | High | Resolved | Đã gọi API `GET /api/v1/trips/active` khi load trang để khôi phục state chuyến đi, tích hợp lắng nghe Socket vị trí Driver chạy trên Map, fetch lại info driver khi có Status Update, và xóa bỏ mock. |
| BUG-010 | 2026-08-20 | FE | Nút sample Customer/Driver không phản ánh dữ liệu database thực tế và login trước đây tự dựng user/profile/wallet từ JWT khi response thiếu user. | High | Resolved | Sample flow thử login trước, chỉ gọi API register khi 401, sau đó login lại và lưu user/token thật; thiếu response hợp lệ sẽ báo lỗi thay vì dựng dữ liệu giả. |
| BUG-011 | 2026-08-20 | FE | Register gửi ảnh base64 nguyên kích thước trong JSON, có thể làm request vượt giới hạn body và làm đăng ký lỗi. | High | Resolved | Ảnh được resize/nén còn tối đa khoảng 30KB mỗi ảnh trước khi gửi; dữ liệu text cũng được trim/normalize và bắt lỗi API theo response contract. |
| BUG-012 | 2026-08-20 | FE | Customer/Driver còn fallback giá, địa chỉ, hồ sơ xe, online state và timer tự chuyển trạng thái khi API/WebSocket chưa trả dữ liệu. | Critical | Resolved | Đã loại bỏ mock nghiệp vụ, hiển thị trạng thái loading/empty/error và chỉ cập nhật state sau dữ liệu thật. Đã gỡ Dev Simulator khỏi app runtime. |
| BUG-013 | 2026-08-20 | FE | Hủy chuyến chỉ reset state local; modal nhận cuốc và dashboard cùng gọi API accept gây request duplicate/race 409. | High | Resolved | Hủy gọi API cancel trước khi reset; dashboard là nơi duy nhất gọi API accept, modal chỉ điều phối callback. |
| BUG-014 | 2026-08-20 | FE/Socket | Customer hủy chuyến nhưng phía driver vẫn hiện popup nhận cuốc. Nếu driver đã nhận cuốc (chưa IN_TRANSIT) mà customer hủy, app driver không có phản hồi nào. Do `DriverDashboardPage.tsx` thiếu các listener cho `driver:trip_cancelled_offer` và `trip:status_changed`. | High | Open | Backend đã được fix để emit đủ 2 event này trong `cancelTrip`. Cần Frontend dev bổ sung 2 listener trên tại màn hình DriverDashboard để tự động clear `incomingOffer` và `activeTrip`. |
| FEAT-001| 2026-08-20 | FE | Môi trường test 1 Khách - Nhiều Tài xế: Các tab trình duyệt bị dính chung chung session do Zustand AuthStore dùng `localStorage`. Khi 1 tài xế bấm nhận cuốc thành công, các tài xế khác đang mở popup không bị đóng popup. | Medium | Open | Backend đã fix luồng `acceptTrip` phát sự kiện `driver:trip_cancelled_offer`. Cần Frontend: 1. Đổi AuthStore sang `sessionStorage` để test multi-tab độc lập. 2. Bắt sự kiện `driver:trip_cancelled_offer` để tự tắt popup nếu có tài xế khác nhanh tay hơn. |
| FEAT-002| 2026-08-20 | FE | Đồng bộ giả lập xe chạy: Backend đã tự động kích hoạt `SimulatorService` ngay khi tài xế nhận cuốc, liên tục phát tọa độ xe chạy và tự động cập nhật trạng thái cuốc xe (từ đón khách đến hoàn thành). | Medium | Open | Frontend (Tài xế) hiện đang dùng các nút bấm thủ công để chuyển trạng thái. Cần cập nhật Frontend Driver để lắng nghe sự kiện `trip:status_changed` và `trip:location_stream` giống như app Khách hàng, đồng thời ẩn/vô hiệu hóa các nút bấm chuyển trạng thái thủ công khi đang chạy giả lập. |
